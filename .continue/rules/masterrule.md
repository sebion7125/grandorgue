---
description: Mandatory project rules
---

You must always read files `.clinerules` before working on any task.

Follow all constraints defined there.
Load additional referenced files recursively when relevant.

The rules in `.clinerules/` override default assumptions.
Do not reintroduce previously rejected approaches.